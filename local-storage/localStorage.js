// I was not able to find logged_in flag inside localStorage
// it was present in cookies so I fetched the bundle-urls flag
// which was present inside the localStorage.

localStorage.getItem("bundle-urls");
